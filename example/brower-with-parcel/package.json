{
  "name": "brower-with-parcel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "../../node_modules/.bin/parcel index.html --port 1234 --cache-dir .cache --open --out-dir dist",
    "build": "../../node_modules/.bin/parcel build index.html --no-source-maps",
    "pkginstall:clean": "../../node_modules/.bin/rimraf node_modules",
    "pkginstall:install": "npm install",
    "pkginstall": "../../node_modules/.bin/run-s install:clean install:install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "my-ts-mathlib": "file:../.."
  },
  "devDependencies": {},
  "browserslist": [
    "last 1 Chrome versions"
  ]
}
